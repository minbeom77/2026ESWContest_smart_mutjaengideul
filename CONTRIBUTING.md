# 협업 규칙

기준 문서: [팀 Notion Convention](https://app.notion.com/p/Convention-d4b205a57c2a4f1790a081d33c42ddc5)

## 브랜치와 제출

1. 작업 시작 전 팀 저장소 main의 최신 변경을 가져옵니다.
2. 본인 계정으로 fork한 저장소에 feature/기능명 브랜치를 만듭니다.
3. 개인 브랜치에서 작업·검사하고 개인 fork에 push합니다.
4. 팀장 저장소 main을 대상으로 PR을 만듭니다.
5. 최소 한 명의 팀원이 코드와 실행 결과를 검토한 뒤 merge합니다.
6. merge 후 main에서 정상 동작을 확인합니다.

main은 최종 배포, develop은 통합 개발 브랜치입니다. main에 직접 commit·push하지 않습니다. 이 모듈의 작업 브랜치는 feature/wifi-csi-sensing입니다.

Commit 제목은 Feat:, Fix:, Refactor:, Docs:, Style:, Test:, Chore: 중 해당하는 접두어를 사용합니다.

## 코드와 검증

Python 변수·함수는 snake_case로 작성하고 PEP 8 형식을 따릅니다. Qt의 closeEvent처럼 라이브러리가 요구하는 재정의 이름은 유지합니다. 함수는 역할을 분명히 하고 주요 처리에 짧은 주석을 붙입니다.

하드웨어 없이도 검증할 수 있는 Dummy Data 검사를 제공합니다. 합성 시험 결과와 실제 센서 정확도는 구별해 기록합니다. 작은 기능 단위로 작업하고 자주 검사합니다.

## 통신·환경·정보 관리

기존 MQTT Topic 이름, JSON Key와 데이터 구조를 임의로 바꾸지 않습니다. 새로운 필드나 규격 변경은 팀과 협의하고 문서를 갱신합니다.

공통 Python 버전과 requirements를 유지하며 새 라이브러리는 팀에 공유 후 적용합니다. 모듈의 기존 검증 환경을 기록하되 이를 팀 공통 환경으로 임의 지정하지 않습니다.

API Key·Secret·개인정보는 코드나 GitHub에 올리지 않습니다. 필요한 비밀 설정은 환경변수 또는 로컬 .env로 관리하고 Git에서 제외합니다.

## 문서와 회의

기능·MQTT·API·구조·일정이 바뀌면 Notion 문서에 날짜·시간과 변경 내용을 함께 기록합니다. 코드와 설명을 함께 검토합니다.

회의 후 결정 사항, 담당자, 다음 작업, 문제와 해결 방법을 기록합니다. 회의는 주 1~2회 진행하며 안건을 미리 준비합니다. 문제가 있거나 새 아이디어가 생기면 팀과 공유합니다.
